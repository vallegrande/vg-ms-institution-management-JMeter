services:
  postgres:
    image: postgres:16-alpine
    container_name: sigei-institutions-postgres
    environment:
      POSTGRES_DB: sigei_institutions
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init-db.sql:/docker-entrypoint-initdb.d/init-db.sql
    networks:
      - sigei-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  
volumes:
  postgres_data:
    driver: local

networks:
  sigei-network:
    driver: bridge